package com.penelopef.models;

public interface MessageList {
    void drawMessage(int i);

    void drawMessageList();
}

/* Implementations:
 * Dashboard
 * MessageThread (a message followed by its replies
 * Mailbox
 * ...
 */
